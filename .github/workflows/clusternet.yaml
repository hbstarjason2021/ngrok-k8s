name: clusternet

on:
  workflow_dispatch: 
  #schedule:
  #  - cron:  0 9,12,21 * * * 
      # 
      # https://tool.lu/crontab/
      # https://datetime360.com/cn/utc-cst-china-time/
  #watch:
  #    types: started   

jobs:
  clusternet:
    runs-on: ubuntu-latest
    #if: github.event.repository.owner.id == github.event.sender.id
    # https://p3terx.com/archives/github-actions-manual-trigger.html
    
    steps:
    #- uses: actions/checkout@v2
    
    - name: clusternet
      run: |
         sudo timedatectl set-timezone 'Asia/Shanghai'
         
         wget https://raw.githubusercontent.com/hbstarjason2021/ngrok-k8s/main/init-tools.sh && \
              bash init-tools.sh
         
         wget https://raw.githubusercontent.com/clusternet/clusternet/main/hack/local-running.sh && \
              bash local-running.sh
         
         echo "==============================="
         
         kubectl config get-contexts
         
         echo "==============================="
         current_time=$(date +"%Y-%m-%d-%k")
         echo ${current_time}
         echo "==============================="
